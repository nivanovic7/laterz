import styles from "./Header.module.css";

function Header() {
  return (
    <header className={`${styles.header} d-grid-custom`}>
      Header placeholder
    </header>
  );
}

export default Header;
